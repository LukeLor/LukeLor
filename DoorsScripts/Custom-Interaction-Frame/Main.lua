-- Currently gonna try obtaining the stuff necessary 

local Module = {}

Module.SetUp = function(Ftype, AssetId)
if string.lower(Ftype) == "interaction" then
 --Main code here, change workspace Ui prompt, and Ui prompt
  elseif string.lower(Ftype) == "item"
    --only change UI item trigger icon
  end
end

return Module
